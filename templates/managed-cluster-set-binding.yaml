{{- range .Values.clusterSets }}
---
apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSetBinding
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.global.namespace }}
spec:
  clusterSet: {{ .name }}
{{- end }}
